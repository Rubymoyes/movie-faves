<main class="features grid">

    <section class="top-movies">
        <h2>Top Voted Flicks</h2>

        {{#each movies}}
        {{>movie-summary}}
        {{/each}}

    </section>

    <section class="categories">
        <h2>Categories</h2>
        {{#each categories}}
        {{>category-summary}}
        {{/each}}

    </section>

</main>